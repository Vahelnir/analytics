{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "pnpm run \"/build:.*/\"",
    "build:type-check": "tsc --noEmit",
    "build:build": "esbuild src/main.ts --bundle --packages=external --platform=node --format=esm --outdir=dist --sourcemap",
    "build:type-gen": "tsc --declaration --emitDeclarationOnly",
    "dev": "tsx watch src/main.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^8.2.0",
    "@trpc/server": "^10.13.2",
    "fastify": "^4.13.0",
    "mongoose": "^7.0.0",
    "superjson": "^1.12.2",
    "zod": "^3.20.6"
  },
  "devDependencies": {
    "@types/node": "^18.14.2",
    "esbuild": "^0.17.10",
    "tsx": "^3.12.3",
    "typescript": "^4.9.3"
  }
}
